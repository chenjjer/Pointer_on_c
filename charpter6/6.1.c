char *find_char(char const *source, char const *chars)
{
	if( chars == NULL || source == NULL)
		return NULL;
	for(int i = 0; i<lenth(sizeof(source); i++) {
		for(int j= 0 ;j<lenth(sizeof(chars);j++) {
			if(source[i] == chars[j])
				return *source[i];
			else
				return NULL;
			}
		}
	}
				
char *find_char(char const *source, char const *chars)
{
	char *cp;
	if(str != NULL && chars != NULL) {
		for(; *str != '\0'; str++) {
			for(cp = chars; *cp != '\0'; cp ++)
				if (*str == *cp)
					return str;
		// there is a another method fing the compare string
		for(cp = chars; *cp != '\0';)
				if( *str == *cp ++£©
					return str;
		}
	}
		return NULL;
}
