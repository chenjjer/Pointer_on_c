#include <string.h>
#include <stdio.h>

void
dollars(char *dest, char const *src)
{
	 char *temp;
	 int count;
	 count = 0;
	 temp = src;
	 while ( *src != NULL)
	 count ++;
	 if(count <= 2 && count >= 0)
	 
	 	 	
	 		
	 		
}